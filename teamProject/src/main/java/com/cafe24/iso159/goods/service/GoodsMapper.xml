<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cafe24.iso159.goods.service.GoodsMapper">
<!-- 	
			회원정보 리스트 전체조회 쿼리
	<select id="selectMemberList" parameterType="com.cafe24.iso159.goods.service.Goods"
		resultType="com.cafe24.iso159.goods.service.Goods">
		SELECT 
		point_goods_code
			, m_admin_id
			, point_goods_name
			, point_goods_point
			, point_goods_desc
			, point_goods_date 
		FROM t_point_goods;
	</select> -->
	<!-- 동물 등록 쿼리 --> 
	<insert id="insertGoods" parameterType="com.cafe24.iso159.goods.service.Goods">
		INSERT INTO t_point_goods 
			(point_goods_code
			, m_admin_id
			, point_goods_name
			, point_goods_point
			, point_goods_desc
			, point_goods_date)
		VALUES (
			#{pointGoodsCode}
			, #{mAdminId}
			, #{pointGoodsName}
			, #{pointGoodsPoint}
			, #{pointGoodsDesc}
			, now());
	</insert>
	<!-- 상품등록 Code 생성 쿼리 -->
	<select id="selectGoodsCodeNumber" resultType="int">
		SELECT
		MAX(cast(substring(point_goods_code, 18) as DECIMAL))
		FROM 
			t_point_goods
	</select>
	

</mapper>